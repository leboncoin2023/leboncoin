<!DOCTYPE html>

<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
       
        <!-- SWIPER -->
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.min.css"/>
        <!-- SWIPER -->
     
        <link rel="stylesheet" href="{{ asset('/assets/css/DH_18_07_style_new.css') }}">

        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('./assets/css/general.css') }}">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">


        
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

    </head>
    <body>
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class=" position-fixed top-50 start-50 translate-middle p-3" style="z-index: 11">
                    <div id="liveToast" class="  popup" role="alert" aria-live="assertive" aria-atomic="true">
                        <div class="popheader">
                            <button type="button" class="btn-close closepopup" data-bs-dismiss="toast" aria-label="Close"></button>
                        </div>
                        <div class="toast-body">
                        <strong>{{ message }}</strong>
                            {% if message  == 'Votre offre a bien été enregistrée.' %}
                                <p class="popupstatus">Nous la transmettons auprès du vendeur.<br>
                                Vous serez informé si une nouvelle enchère intervient.</p>
                            {% else %}
                                <p class="popupstatus">Votre enchère ne peut être prise en compte.<br>
                                Pensez à vérifier le montant actuel de l'enchère.</p>
                            {% endif %}
                            <button type="button" class="btnpopup closepopup" data-bs-dismiss="toast" aria-label="Close">Ok</button>
                        </div>
                    </div>
                </div>
            {% endfor %}
        {% endfor %}
        {% include 'base/header.html.twig' %}
        {% block body %}
        
          
                

            
                
   
{% endblock %}
        
        {% include 'base/footer.html.twig' %}
        <script src="https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.min.js"></script>      
        
        <script src="/assets/js/toast.js"></script>
    </body>
        

 {% block javascripts %} 
               
    {% endblock %}
           

</html>



